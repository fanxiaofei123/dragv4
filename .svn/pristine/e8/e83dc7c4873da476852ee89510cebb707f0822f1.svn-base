package org.com.drag.dao;

import org.com.drag.model.Template;

import java.util.List;

public interface TemplateDao extends BaseDao<Template> {
    List<Template> selectAllByUserId(Template template);

    Integer deleteById(String[] templateId);

    Integer insertTemplate(Template template);

    List<Template> selectAllByUserIdVim(Template template);
}